<!-- if a title exists, add it to the h1
If not return to the base view. -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<!-- Display error messages (if any) -->
<%- messages() %>

<!-- Display the vehicle details content -->
<%- vehicleHtml %>